name: 'streamsense_ai'
version: '1.0.0'
config-version: 2

profile: 'streamsense_ai'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  streamsense_ai:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
